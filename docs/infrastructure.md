# LAYERPITCH — INFRASTRUCTURE
*Extrait de `MASTER.md` le 28 juillet 2026 lors de la restructuration en fichiers spécialisés. Partie A inchangée sur le fond. Partie B mise à jour le 27 août 2026 — canal dédié à la bascule backend, cinq décisions d'architecture actées avant tout code ; corrigée le 29 août 2026 (schéma `loops`/`segment_slots`, Supabase managé vs auto-hébergé) ; complétée le 31 août 2026 (CORS ajouté à la Décision 3, trouvé au test de l'étape 1 implémentée ; vérification admin `invite-tester` ajoutée à la Décision 4, étape 2 implémentée ; étape 3 — base Postgres — implémentée et vérifiée ; Décision 2 — couche api/*.js/RPC — implémentée et vérifiée ; étape 4 — logique d'achat Stripe — implémentée et vérifiée, les quatre étapes de la Décision 5 sont posées). Partie C ajoutée le 29 août 2026 — protection d'accès du repo bêta.*

## Vue d'ensemble — trois chantiers de nature différente

- **Partie A — Bêta de retour qualitatif** (décidée le 12 juillet, en cours) : ne nécessite aucun développement backend.
- **Partie B — Bascule backend réelle** (cadrée le 12 juillet, architecture actée le 27 août, développement pas encore commencé) : comptes, base de données, paiement — pour une version ultérieure du produit.
- **Partie C — Protection d'accès du repo bêta** (décidée le 27 août, mise en œuvre en cours depuis le 29 août) : verrouillage de l'accès au site GitHub Pages actuel via Cloudflare Zero Trust Access, indépendant de la bascule backend.

**Changement de calendrier (27 août)** : la roadmap datée dans `business-marche.md` visait un démarrage "début 2027" pour la Partie B. Décision volontaire d'avancer ce chantier en tâche de fond dès maintenant, en parallèle du reste — motivée par l'avancement du travail de fond, pas par l'urgence de sécurité (celle-ci est traitée séparément via Cloudflare Access, Partie C).

## Partie A — Bêta de retour qualitatif

### Objectif et contrainte

Faire tester LayerPitch à une dizaine de compositeurs (ordre de grandeur, non figé), qui créent leurs propres AdReels et donnent des retours d'usage. Contrainte actée : minimiser au maximum la friction d'entrée — pas question de demander au testeur de comprendre ou manipuler GitHub lui-même.

### Solution retenue — duplication de repo préconfigurée, sans backend

Pour chaque testeur : duplication du repo GitHub existant, `data.json` de départ préremplit (état d'exemple), génération d'un **token GitHub fine-grained restreint à ce seul repo**, transmission d'un lien direct vers son propre backstage déjà fonctionnel. Le testeur ne sait jamais que GitHub existe derrière.

**Pourquoi cette solution plutôt qu'un vrai système de comptes** : zéro développement backend nécessaire, isolation naturelle sans risque (repo par testeur, pas de fuite de données entre testeurs), correspond exactement à l'objectif (retours qualitatifs), pas à un vrai lancement produit.

**Note de transition (27 août)** : ce système sera remplacé par l'authentification Supabase (Partie B, Décision 4) une fois la bascule backend effective — mais reste inchangé et pleinement fonctionnel jusque-là. Aucune régression entre-temps.

### Précautions opérationnelles

- Token GitHub distinct et restreint par testeur — jamais un token donnant accès au repo principal ou à ceux des autres testeurs.
- Tous les repos de bêta hébergés sous une **organisation GitHub dédiée** (`layerpitch-beta`), séparée du compte personnel — facilite le nettoyage en fin de bêta (suppression de l'organisation entière).

### Limite assumée

Pas de tableau de bord unifié listant tous les testeurs — chaque repo consulté séparément. Adapté jusqu'à une dizaine de testeurs, pas à une ouverture à grande échelle ; revoir l'approche si le nombre visé dépassait significativement la dizaine.

### Outillage — `layerpitch-beta-sync.js` (Node.js, `GH_TOKEN` en variable d'environnement)

Trois commandes :
- **`promote`** — copie les fichiers moteur (`index.html`, `pack.html`, `player.js`, `layerpitch-backstage.html`) vers `layerpitch-beta-template` (repo "Template repository" côté GitHub), régénère automatiquement un `data.json` de départ (mêmes types de blocs que l'AdReel principal, vidés de contenu).
- **`create <nom>`** — crée `layerpitch-beta-<nom>` depuis le template, remplace le repère `__TESTER_REPO__` du backstage par le vrai nom.
- **`rollout`** — pousse une mise à jour des fichiers moteur vers tous les repos testeurs existants.

**Cache navigateur/CDN — résolu le 13/08** : après publication depuis le backstage, les changements pouvaient ne pas apparaître de façon fiable côté public sans vidage manuel du cache. Cause : absence de cache-busting sur `data.json` et sur les scripts moteur partagés. État actuel :
- `data.json` : chargé avec `?v=' + Date.now()` dans `index.html`/`pack.html`/`collection.html` — requête réseau fraîche à chaque chargement de page, jamais de cache, ni navigateur ni CDN.
- `player.js`, `layerpitch-i18n.js`, `layerpitch-help.js` : leurs balises `<script>` sont réécrites à chaque publication avec `?v=<buildVersion>` (même timestamp que `publishedAt`), via `updateScriptVersions()` — un seul fetch+écriture par fichier concerné (`index.html`, `pack.html`, `collection.html`, `video-test.html`, `layerpitch-backstage.html`).
- **Résiduel, hors de notre contrôle** : le TTL de 10 min de GitHub Pages (non configurable) s'applique toujours à l'URL canonique de la page HTML elle-même (celle du lien partagé, qui ne change jamais). Sans conséquence sur le *contenu* affiché (toujours piloté par `data.json`, toujours frais), seulement sur le *code* (nouvelle fonctionnalité, changement de structure) — s'autorésout tout seul en 10 min maximum après une publication.

## Partie B — Bascule backend (architecture actée le 27 août, développement non commencé)

**Stack retenue** : Supabase (Postgres + auth), Cloudflare R2 (média, egress-free) — Supabase réservé au Postgres. Couche d'abstraction API fine (service layer JS côté client, pas de serveur dédié) entre le produit et Supabase, pensée pour la réversibilité. Sécurité en deux niveaux : validation applicative + Postgres RLS.

**Estimation de temps** : ~46-74 jours de temps fondateur piloté par IA pour les étapes 1 à 3 (**estimation**, pas un fait vérifié — posée le 14 juillet, non révisée depuis).

### Décision 1 — Schéma de données Postgres

**Approche retenue : hybride.** Tables relationnelles pour les entités de premier niveau et tout ce qui porte une vraie logique de graphe ou de transaction ; colonnes JSONB pour les sous-structures propres à un mode de lecture, sans intégrité référentielle critique.

**Tables "dures" (avec contraintes/FK réelles)** :
- `tracks`, `track_folders`
- `segment_slots` — **spécifique au mode `sequential`** (fait vérifié le 29 août sur un cas réel : le mode `embranchement-vertical` n'utilise pas cette structure, voir correction plus bas). FK `track_id` ; FK auto-référentielle `references_slot_id` (`ON DELETE SET NULL`, cohérent avec le comportement actuel du backstage qui nettoie déjà cette référence à la suppression)
- `segment_slot_transitions` — les `nextOptions[]` du mode `sequential` uniquement ; FK `from_slot_id` et `target_slot_id` vers `segment_slots.id` (`ON DELETE CASCADE` : une option de branchement qui pointe vers un segment supprimé disparaît proprement avec lui, plutôt que de laisser un bouton mort dans un AdReel publié)
- `sfx_library`, `sfx_folders`
- `packs`, `collections`, `ad_reels`, `ad_reel_folders`
- `albums` — entité séparée de `packs` (décision de fond actée le 31 juillet dans `extensions-roadmap.md` 5.5 : différence d'audience et de finalité commerciale, pas un simple champ `type`). Réutilise le moteur de lecture adaptative (mêmes `tracks` référencés), champs éditoriaux propres (`presentationFr/En`, réutilisation du champ existant côté Pack).
- `pack_purchases`, `album_purchases` — achat unitaire (one-time payment, pas d'abonnement), `buyer_id` → `profiles`, `pack_id`/`album_id`, `purchased_at`, `price_paid`, `stripe_payment_intent_id` (nullable, en attente de la Phase 1 paiement)
- `profiles` — une ligne par compte (voir Décision 4), sans `role` figé
- `composer_profiles`, `buyer_profiles` — profils optionnels rattachés à un compte, activables/cumulables (modèle "un compte, plusieurs profils" façon YouTube Studio, acté le 30 juillet dans `extensions-roadmap.md` 5.4)
- `plan_quotas` — quotas par palier (`free`/`starter`/`pro`), table plutôt que valeurs codées en dur, ajustable sans migration
- Tables de liaison pour les listes d'IDs référencés (`pack_tracks`, `collection_packs`, `ad_reel_tracks`, etc.) plutôt que des tableaux d'IDs bruts, pour garantir l'intégrité sans code de validation supplémentaire
- `socials`, et une table "réglages globaux" (une seule ligne : `publishedAt`, `implementationSkills`, `noAiCertifiedGlobal`, `customFonts`)

**Colonnes JSONB (souple, propre au mode, pas de graphe critique)** : `layers`, `sections` (avec `pools[]` imbriqués, y compris `referencesPoolId`/`referencesSectionId` en référence molle non contrainte — cohérent avec leur usage actuel), `loops` (mode `embranchement-vertical` — pas de `targetId`/graphe à valider, la bascule entre boucles ne référence aucune cible par identifiant), `alternatives`, `intro`/`outro`, `profile`/`theme`, `testimonials`, `blocks`, `trackOverrides`.

**Correction du 29 août** : le nom du mode `embranchement-vertical` avait fait supposer, à tort, qu'il partageait la mécanique `segmentSlots`/`nextOptions` du mode `sequential`. Un premier morceau réel testé dans ce mode a confirmé que sa structure est en réalité un tableau `loops[]` indépendant (`isInitial`, `isDetour`, `detourMode`, `autoReturnEnabled`, `switchQuantize`, `transition` porté directement par la boucle) — vérifié à la fois dans les données publiées et dans `player.js` (`nextOptions`/`targetId`/`referencesSlotId` n'existent que pour `segmentSlots`). `loops` déplacé en JSONB en conséquence, `segment_slots`/`segment_slot_transitions` recentrées explicitement sur le seul mode `sequential`. Exemple concret rencontré au passage de l'intérêt du JSONB pour ces structures : un nouveau champ `originalFileName` a été ajouté début pour toutes les entités porteuses de fichier (couches, boucles/transitions embranchement-vertical, etc.) sans nécessiter de migration de schéma, les enregistrements existants continuant simplement d'en être dépourvus.

**Correction du 31 août — `owner_id` manquant sur le contenu compositeur** : oubli de cette décision, pas une extension. LayerPitch est multi-compositeur depuis la conception (business plan, et déjà en vigueur dans la Partie A où chaque testeur bêta a son propre repo isolé) ; le modèle de comptes à profils multiples (Décision 4) et les paliers `free`/`starter`/`pro` par compte n'auraient aucun sens pour un seul compositeur. Le schéma initial créait `composer_profiles` sans jamais rattacher le contenu à un propriétaire. Corrigé : `owner_id` (FK vers `composer_profiles`) ajouté sur `tracks`, `track_folders`, `sfx_library`, `sfx_folders`, `packs`, `collections`, `albums`, `ad_reels`, `ad_reel_folders` — colonnes `not null`, contenu existant rattaché au compte de Jules-Antoine (seul compositeur à ce jour). Les tables de liaison (`pack_tracks`, `track_sfx`, etc.) et `segment_slots`/`segment_slot_transitions` n'ont pas leur propre `owner_id` : elles héritent de la propriété de leur table parente. Lecture publique inchangée (le site reste consultable sans compte) ; le contrôle porte sur l'écriture, appliqué dans les RPC `upsert_*` (voir Décision 2).

**Colonnes provisionnées vides, en attente d'un chantier de conception ultérieur mais déjà destination actée** :
- `packs.tags` (tableau de texte) — pour la vraie Marketplace organisée (catalogue parcourable, filtres), Phase 2, une fois qu'il y aura assez de packs en vente pour la justifier.

**Explicitement hors périmètre du schéma actuel** (destination non tranchée, pour éviter de deviner un modèle pas encore pensé) : tout ce qui touche à Moodboard Studios (nom et modèle de rémunération studio exclus de toute communication externe, cf. règle de canal) ; `playlists`, `playlist_tracks`, `user_track_settings`, et toute table liée au bouton "Figer" (exports) — la conception de `extensions-roadmap.md` 5.5 reste incomplète sur ces pans ("Playlists et Figer restent à finir").

### Décision 2 — Couche d'abstraction API

**Pas de serveur API séparé** (type NestJS/FastAPI) — disproportionné pour un solo founder qui ne code pas lui-même.

**Architecture retenue** :
```
Front statique (index.html, backstage, player.js)
        │
        ▼
  api/*.js  ← couche d'abstraction (service layer)
   ├── api/auth.js       → wrappe supabase.auth.* (isolé en priorité — point de sortie le plus "collant" de Supabase)
   ├── api/tracks.js      → CRUD simple via SDK Supabase
   ├── api/packs.js       → idem
   ├── api/adreels.js     → idem
   └── api/publish.js     → appelle des RPC (fonctions Postgres versionnées en SQL)
        │
        ▼
  Postgres (RLS + fonctions RPC pour la logique métier : intégrité segment_slots, publication atomique)
```

- **RPC (fonctions Postgres, PL/pgSQL)** pour toute logique métier proche des données : validation du graphe `segment_slots`/`nextOptions`, publication atomique multi-tables. Plus rapide qu'un aller-retour réseau supplémentaire, versionné comme SQL dans les migrations (réversibilité maximale).
- **Edge Functions réservées** aux seuls cas nécessitant un secret serveur ou un appel externe : envoi d'invitation bêta-testeur (`invite-tester`, utilise la clé secrète Supabase Auth Admin, ne doit jamais toucher le client), futurs webhooks Stripe. Aucun besoin à ce stade au-delà de l'invitation.
- **RLS Postgres** pour la sécurité d'accès — logique de permission en SQL versionné, portable par nature (`pg_dump`).

**Implémenté et vérifié le 31 août** : `api/tracks.js`, `api/packs.js`, `api/adreels.js` (lecture directe via SDK + imbrication PostgREST, écriture exclusivement via RPC) et les RPC `upsert_track`/`upsert_pack`/`upsert_ad_reel`. `upsert_track` valide le graphe `segmentSlots`/`nextOptions` avant écriture — testé (8/8 via `pg`, confirmé aussi via un vrai chemin navigateur/PostgREST après correction de deux bugs trouvés à ce test : ambiguïté PostgREST sur la double FK `segment_slot_transitions.from_slot_id`/`target_slot_id`, et GRANT `SELECT` manquants pour `anon`/`authenticated` — les tables créées par connexion Postgres directe n'avaient pas hérité des privilèges par défaut habituellement posés par le dashboard Supabase). `api/publish.js` (orchestration multi-entités façon "Sauvegarder/publier" actuel) volontairement non construit — pas nécessaire tant que le site public ne dépend pas de Postgres (Décision 5). **Correction du 31 août (soir)** : l'autorisation des RPC reposait sur `is_admin()`, un admin unique en dur (même interim que `invite-tester`, Décision 4) — documenté dès l'origine comme provisoire "en l'absence de `profiles`/rôle". Une fois `composer_profiles` réellement peuplé (voir correction `owner_id`, Décision 1), remplacé par un vrai contrôle de propriété : chaque RPC vérifie que l'appelant a un `composer_profile`, devient propriétaire à la création, et ne peut modifier que ses propres lignes (`current_composer_id()`, testé 11/11 avec un second compte de test simulant un compositeur distinct). `is_admin()` retiré, plus aucun appelant. **RPC `upsert_sfx`/`upsert_collection` ajoutées le 1er septembre** (manquantes jusque-là, construites au démarrage de la Session C) — même schéma exact (`current_composer_id()`, junctions gérées atomiquement pour `upsert_collection`/`collection_packs`). Détail complet : `docs/LAYERPITCH_CHANGELOG.md`.

### Décision 3 — Migration des médias vers Cloudflare R2

- **Un seul bucket** (`layerpitch-media`), distinction par préfixe de clé plutôt que par bucket.
- **Structure de clés miroir de l'actuelle** : `audio/<trackId>/<filename>`, `sfx/<sfxId>/<filename>`, `images/<packId ou adReelId>/<filename>` — réécriture d'URL quasi mécanique (changement du seul domaine de base).
- **Accès public via domaine personnalisé `media.layerpitch.com`** — nécessite que `layerpitch.com` soit une zone gérée par Cloudflare (nameservers pointés dessus). **Point bloquant levé le 29 août** : le domaine est acheté et sa zone en cours de bascule vers Cloudflare (voir section Domaine ci-dessous) — l'exposition publique réelle du bucket (cache, WAF, URL stable) pourra être configurée une fois la zone confirmée active. Le `r2.dev` temporaire n'est plus nécessaire même pour les tests, dès que la zone est active.
- **Pas de presigned URLs / auth par objet à ce stade** — cohérent avec le modèle de sécurité par obscurité déjà en place aujourd'hui (ID de dossier non devinables dans les chemins GitHub Pages actuels), réévaluable une fois Cloudflare Access branché (Partie C).
- **Migration = copie 1:1** des fichiers `images/` et `audio/` du repo GitHub vers le bucket, chemins conservés.
- **CORS requis sur le bucket** (trouvé au test visuel du 31 août, absent de cette décision initiale) : `player.js` charge l'audio via `fetch()` + `decodeAudioData` (Web Audio API, nécessaire au bouclage sample-accurate), soumis à la vérification CORS du navigateur dès que le site n'est pas sur le même domaine que `media.layerpitch.com` — contrairement aux images (`<img src>`, jamais concernées). Policy posée manuellement via le dashboard Cloudflare (R2 > bucket > Settings > CORS Policy) : `AllowedOrigins: ["*"]`, `GET`/`HEAD` — cohérent avec l'absence de presigned URLs déjà actée ci-dessus, et sans coût lié à l'origine (R2 egress-free). Le token R2 scope "Object Read & Write" ne suffit pas pour cette opération de niveau bucket (403) ; nécessite soit le dashboard, soit un token "Admin Read & Write".

### Décision 4 — Authentification

- **Supabase Auth, connexion par magic link (passwordless)** — pas de mot de passe, adapté à des testeurs non-techniques, cohérent avec "aucun processus de paiement à ce stade".
- **Inscriptions publiques désactivées** — seuls les comptes créés via invitation admin peuvent se connecter. Bascule vers inscription publique = simple changement de configuration au moment de l'ouverture grand public, pas une refonte.
- **Invitation envoyée via l'Edge Function `invite-tester`**, appelée depuis `api/auth.js`.
- **Modèle de comptes : un seul compte, plusieurs profils cumulables** (acté le 30 juillet dans `extensions-roadmap.md` 5.4, façon YouTube/YouTube Studio) — `profiles` (une ligne par compte) + `composer_profiles`/`buyer_profiles` optionnels et non exclusifs. Un compositeur qui achète aussi des packs a les deux profils sur le même compte.
- **Point de vigilance opérationnel** (implémentation, pas architecture) : la limite par défaut de 2 emails d'invitation/heure de Supabase nécessitera soit un SMTP personnalisé, soit un espacement des invitations lors du rollout bêta. **Confirmé en pratique le 31 août** (`email rate limit exceeded` reçu lors du test de `invite-tester`), **reconfirmé le 1er septembre** en testant la connexion par lien magique dans le backstage (Session B) — atteinte avec un seul compte en une heure de tests, pas seulement en rafale d'invitations multiples. **À traiter avant l'ouverture réelle aux testeurs (Session C/D), pas avant** : configurer un SMTP personnalisé côté dashboard Supabase (Resend/Postmark/SendGrid, plan gratuit suffisant pour une dizaine de testeurs) — retire la limite par défaut entièrement, pas seulement un espacement manuel. Réglage dashboard, pas du code (même nature que le CORS R2 posé manuellement par Jules-Antoine en août).
- **Qui peut appeler `invite-tester` (trouvé au moment de l'implémentation du 31 août, absent de cette décision initiale)** : pas de table `profiles`/rôle disponible tant que l'étape 3 (base Postgres) n'est pas faite, donc pas de vraie vérification de rôle possible pour l'instant. Interim retenu : la fonction vérifie que l'appelant est authentifié ET que son email correspond au secret `ADMIN_EMAIL` (Jules-Antoine uniquement) — à durcir une fois `profiles` en place (changement contenu à l'intérieur de la fonction).

### Décision 5 — Ordre de bascule

Migration incrémentale (pattern "Strangler Fig"), pas de big bang — chaque étape validée en production avant la suivante, le prototype actuel reste fonctionnel à chaque étape :

1. **Médias vers R2** — isolé, aucune dépendance auth/DB, changement du seul champ `base` dans `data.json` une fois copié et vérifié.
2. **Authentification Supabase** — isolable, testable en parallèle de l'usage normal, remplace le système bêta GitHub sans coupure.
3. **Base Postgres + migration des données** — étape la plus délicate. Le script de migration peuple Postgres depuis `data.json` (validation du graphe `segmentSlots`/`nextOptions` incluse) sans faire dépendre le site public de cette base tant qu'un AdReel de test servi depuis Postgres n'a pas été vérifié identique en comportement à l'original. **Schéma appliqué et migration vérifiée le 31 août** (25 tables, RLS activé, fidélité confirmée sur la totalité de `data.json` — au-delà du minimum "un AdReel de test") — voir `docs/LAYERPITCH_CHANGELOG.md` pour le détail. Le site public ne dépend pas encore de cette base (`data.json` reste la source de vérité) : couche `api/*.js` de CRUD applicatif et RPC de validation/publication atomique (Décision 2) restent à construire avant tout basculement réel. **Lecture Postgres côté public construite et validée en isolation le 1er septembre** (`api/sfx.js`/`api/collections.js`/`api/settings.js`/`api/site-data.js` nouveaux, complètent `api/tracks.js`/`api/packs.js`/`api/adreels.js` — parité vérifiée sur la totalité des données, un seul écart réel trouvé : `packs.buyable` désynchronisé entre Postgres et `data.json`, hérité d'une valeur de test de l'étape 4, non corrigé) — activable via `?dataSource=postgres` sur `index.html`/`pack.html`/`collection.html`, **toujours pas la valeur par défaut en production**. La bascule réelle reste conditionnée à ce que le backstage en ligne écrive dans Postgres de façon fiable (sans quoi la publication suivante depuis le backstage GitHub actuel figerait le contenu public). Détail complet : `docs/LAYERPITCH_CHANGELOG.md`.
4. **Logique d'achat** (`pack_purchases`/`album_purchases`, bibliothèque acheteur) — dépend des trois étapes précédentes, dernière pièce. **Implémentée et vérifiée le 31 août** (compte Stripe créé par Jules-Antoine en cours de session) : `create-checkout-session`/`stripe-webhook` (Edge Functions), `api/purchases.js`. Managed Payments (choisi par Jules-Antoine — Stripe gère taxes/fraude/support, 3,5% de frais en plus par transaction) a nécessité plusieurs ajustements (code de taxe produit, version d'API, pas de `payment_method_types` explicite). Paiement réel complété en mode test de bout en bout (carte `4242...`, TVA française calculée automatiquement, webhook confirmé, idempotence vérifiée). Deux bugs de fond trouvés au passage : aucun GRANT `service_role` sur les tables (même cause que le correctif `anon`/`authenticated` de la couche api/*.js, rôle oublié dans ce correctif-là), et aucune ligne `profiles` jamais créée à l'inscription (mécanisme standard `auth.users` → `public.profiles` absent depuis l'étape 3, ajouté). Prix des packs = valeurs de test provisoires, pas une décision de pricing réelle. UI d'achat (`index.html`/`pack.html`) non intégrée cette session — le backend est prêt, pas le visuel. Détail complet : `docs/LAYERPITCH_CHANGELOG.md`.

### Décision complémentaire — Supabase managé vs auto-hébergé (tranchée le 29 août)

**Retenu : Supabase Cloud managé**, pas d'auto-hébergement sur OVH.

- **Coût** : plan gratuit disponible, puis Pro à partir de 25\$/mois (10\$ de crédit de calcul inclus, 8 Go de disque, 100 000 utilisateurs actifs/mois, 250 Go de sortie réseau inclus) — largement suffisant pour la bêta et les premiers mois d'ouverture réelle.
- **Raison de fond** : l'auto-hébergement ne retire pas de travail de code, il ajoute du travail d'exploitation serveur (mises à jour mensuelles, durcissement de sécurité, sauvegardes, surveillance) — hors du périmètre de Jules-Antoine (ne code pas lui-même) et de Claude Code (assistance au code, pas astreinte serveur). Une base auto-hébergée sans sauvegarde hors-site correctement mise en place n'est pas une économie, c'est un risque déguisé.
- **Réversibilité préservée** : Supabase reste du Postgres standard dans les deux cas — un passage à l'auto-hébergement resterait possible plus tard si le volume le justifiait un jour, sans que ce choix initial ne l'hypothèque.

### Décision complémentaire — Approche du backstage en ligne (actée le 31 août)

Réécriture **incrémentale** du fichier local actuel (7600+ lignes), pas un big bang. Ordre suggéré, à affiner en Claude Code selon l'état réel du fichier : lecture d'abord (basculer l'affichage vers `api/tracks.js` etc. sans toucher à l'écriture), puis écriture fonctionnalité par fonctionnalité (`upsert_track`/`upsert_pack`/`upsert_ad_reel`, la plus simple d'abord, un test par fonctionnalité), auth Supabase en dernier — pour ne pas introduire deux inconnues à la fois (nouvelle source de données et nouvelle authentification).

**Précision actée le 1er septembre — écriture double pendant toute la transition, bascule manuelle par Jules-Antoine.** Question soulevée par Jules-Antoine avant de démarrer la conversion de l'écriture (Session B du séquençage) : le modèle "conversion feature par feature" impliquait, tel que posé le 31 août, un gel de la publication réelle sur tout type de contenu déjà converti — le temps que la bascule finale du site public ait lieu (potentiellement toute la durée des Sessions B+C, pas juste B). Jugé trop risqué pour l'usage réel du backstage (démos envoyées à de vrais prospects pendant ce laps de temps). **Retenu à la place** : chaque fonctionnalité convertie écrit simultanément vers Postgres (RPC `upsert_*`, nouveau) **et** vers GitHub/`data.json` (chemin `ghPutFile` existant, non touché) — les deux restent synchronisés automatiquement, sans geler la publication réelle à aucun moment de B ni C. Les deux "versions" du site public cohabitent (celle servie par défaut depuis `data.json`, celle activable via `?dataSource=postgres` depuis la Session A) jusqu'à ce que Jules-Antoine, après avoir lui-même testé la version Postgres en conditions réelles, décide explicitement de la bascule — un seul point de bascule (le comportement par défaut de `loadSiteData()`), jamais automatique ni décidé unilatéralement par Claude Code. L'écriture double peut être laissée active un temps après la bascule, en filet de sécurité pour un retour arrière rapide, à la discrétion de Jules-Antoine. Contrepartie assumée : chaque fonctionnalité convertie en Session B/C demande de vérifier que les deux écritures réussissent (pas seulement une), un peu plus de travail par fonctionnalité mais un coût borné.

### Décision complémentaire — Upload vidéo gameplay par l'acheteur + analytique d'usage (actée le 31 août, simplifiée le 31 août)

**Contexte** : la fonction "mode test gameplay" existante (`videoTestModeEnabled` sur `packs`, `video-test.html`) est aujourd'hui 100 % côté client — la vidéo importée par l'acheteur ne quitte jamais son navigateur, rien n'est enregistré côté serveur. Deux évolutions décidées :

**Analytique d'usage** — table `video_test_events` (`pack_id`, `buyer_id` nullable — un visiteur non connecté peut tester avant de créer un compte —, `event_type` : `started`/`completed`/`abandoned`, `occurred_at`). Deux événements suffisent, pas de mécanisme de retour qualitatif demandé à l'acheteur pour l'instant.

**Upload serveur de la vidéo** — l'acheteur pourra uploader sa vidéo plutôt que de la garder locale au navigateur, **stockage temporaire uniquement** (règle de cycle de vie R2 native, 7-30 jours à affiner). Tous les comptes acheteurs restent gratuits pour l'instant, sans palier payant.

**Correction du 31 août, après clarification** : une première version de cette décision introduisait un palier "studio" payant avec stockage permanent. **Retiré** — ça se rapprochait trop du modèle Moodboard Studios lui-même (abonnement studio payant chez LayerPitch, avec accès élargi) que ce canal doit explicitement laisser de côté sans le deviner. Le stockage vidéo permanent, et plus largement les comptes studios avec leurs fonctionnalités propres (Moodboard, vente d'OST façon Bandcamp), seront traités ensemble comme un chantier de conception dédié le jour venu — pas construits en pièces détachées ici.

**Correction de la Décision 3 pour ce cas précis** : contrairement à l'audio/images du compositeur (publics par obscurité d'URL, décision inchangée), les vidéos gameplay uploadées par un acheteur sont potentiellement du contenu de jeu non publié — **stockage privé, accès uniquement par URL signée temporaire (presigned URL) scopée au buyer propriétaire**, jamais public.

**Schéma retenu, simplifié** :
- `buyer_video_uploads` — `buyer_id` (FK `buyer_profiles`), `pack_id` (FK `packs`), `r2_key`, `original_filename`, `file_size_bytes`, `uploaded_at`, `expires_at` (toujours renseigné, pas nullable — tout upload est temporaire pour l'instant). Pas de champ palier sur `buyer_profiles` à ce stade.

### Décision complémentaire — Packs customs acheteur (schéma formalisé le 31 août)

Conception déjà entièrement documentée dans `extensions-roadmap.md` 5.3 (30 juillet), traduite ici en schéma Postgres :
- `buyer_custom_packs` — table dure : `buyer_id` (FK `buyer_profiles`), `nom`, `date_creation`, `allow_simultaneous_playback` (bool, défaut `false`).
- `buyer_custom_pack_tracks` — liaison : `custom_pack_id`, `track_id`.
- `buyer_custom_pack_sfx` — liaison : `custom_pack_id`, `sfx_id`, `custom_label` (nullable).

Règles héritées de la conception d'origine : un pack custom référence uniquement des assets déjà possédés par l'acheteur (jamais un nouvel objet vendable, pas de republication), peut être partiel (SFX seuls tolérés, sans musique). **Point d'implémentation pour Claude Code** : la RPC d'écriture doit vérifier la possession réelle de l'asset ajouté (via `pack_purchases` → `pack_tracks`) avant d'accepter l'ajout, même logique de validation que la vérification de propriété compositeur du 31 août.

### Décision complémentaire — Lecteur embarquable, par piste et par AdReel (actée le 31 août)

**Cadrage** : le terme "playlist" a d'abord prêté à confusion — clarifié comme "la liste de morceaux d'un AdReel", pas les vraies playlists auditeur de `extensions-roadmap.md` 5.5 (celles-là restent non conçues, hors périmètre inchangé). Rien de nouveau côté schéma : s'appuie entièrement sur `ad_reels`/`ad_reel_tracks`/`tracks` déjà actés en Décision 1.

**Fonctionnement** : une version allégée de la page AdReel, pensée pour l'intégration `<iframe>` sur un site externe — deux variantes, un morceau isolé (`/embed/<adReelId>/track/<trackId>`) ou la liste complète des morceaux de l'AdReel (`/embed/<adReelId>`). Comparable techniquement à l'intégration YouTube/Vimeo déjà existante pour le mode test gameplay des packs.

**Activation** : par défaut pour tout AdReel publié, sans réglage par le compositeur — cohérent avec le fait qu'un AdReel est de toute façon pensé pour être partagé publiquement. Pas de champ `embed_enabled` nécessaire.

### Clarifications — paliers/quotas et tableau de bord analytique compositeur (31 août)

**Paliers/quotas** : `plan_quotas` existe déjà en base avec des valeurs volontairement `NULL` — chiffres à définir par Jules-Antoine (choix de tarification, pas d'architecture), non tranchés dans ce canal.

**Tableau de bord analytique compositeur** : cadré comme "comment" (comportement d'interaction), explicitement pas "qui" (le compositeur connaît déjà ses destinataires) — donc pas de nouveau besoin d'identité nominative, juste un filtrage par `owner_id` de données déjà collectées ou déjà décidées (Umami pour les visites/durée par AdReel, `video_test_events` et événements en jeu existants pour le détail des interactions). Rien de nouveau à construire côté schéma. Distinct de l'assistant IA d'analyse (`extensions-roadmap.md` 3.4, resté hors périmètre) : ceci est un tableau de bord brut, pas une analyse interprétée par IA.

**État réel d'implémentation des comptes multicasquettes** (Décision 4) : vérifié par Claude Code le 1er septembre — schéma seulement. `composer_profiles`/`buyer_profiles` n'apparaissent nulle part dans le code applicatif (`api/*.js`, `index.html`, `pack.html`, `layerpitch-backstage.html`), uniquement dans le schéma SQL et le script de test des RPC. Le rattachement de propriété (`owner_id`) fonctionne et est vérifié, mais aucun flux d'inscription compositeur, aucun sélecteur de casquette, aucun usage de `buyer_profiles` n'existe côté produit. Point de vigilance additionnel trouvé le même jour en recroisant avec `extensions-roadmap.md` 5.4 : le modèle de comptes réel prévoit **trois profils**, pas deux — Fan (par défaut sur tout compte, porte l'entité Album/OST) + Compositeur + Game dev — alors que le schéma Postgres actuel n'a que deux tables de profil. **Tranché par Jules-Antoine le 1er septembre : troisième table** (`fan_profiles`), pas une fusion avec `buyer_profiles` — créée automatiquement à l'inscription (le profil Fan est "toujours présent par défaut", contrairement à `composer_profiles`/`buyer_profiles` qui restent optionnels), même trigger `handle_new_user()` que `profiles`. Usage prévu communiqué : achat d'Albums OST Adaptive Edition. Aucune UI construite — reste un chantier à part entière (`extensions-roadmap.md` 5.5), le nom "Fan" reste provisoire.

### Décision complémentaire — Paliers payants compositeur (`free`/`starter`/`pro`), actée le 1er septembre

Les compositeurs auront eux aussi des paliers payants (`free`/`starter`/`pro`), sur le même principe que les studios — pas seulement un levier de gating pour le moteur de capture vidéo/rescore (voir plus bas), un vrai modèle d'abonnement compositeur en tant que tel. **Chiffres non tranchés** — Jules-Antoine les prépare de son côté (choix de tarification, pas d'architecture) ; `plan_quotas` reste `NULL` en attendant.

**Implication technique nouvelle, pas encore construite** : aucun mécanisme d'abonnement récurrent compositeur n'existe aujourd'hui. Le compte Stripe actuel (Décision 5, étape 4) ne gère que l'achat unitaire côté acheteur (`pack_purchases`, one-time payment) — un flux Stripe Billing (abonnement récurrent) séparé sera nécessaire côté compositeur avant que `plan_quotas` soit réellement appliqué. Aucun RPC ni `api/*.js` ne lit `plan_quotas` à ce jour (vérifié) — la table est provisionnée mais totalement inerte.

### Décision complémentaire — Moteur de capture vidéo de gameplay et rescore (`extensions-roadmap.md` §1), isolé en worktree Git

Le développement du moteur de capture/rendu offline (bounce + mux) décrit dans `extensions-roadmap.md` sera fait dans une worktree Git séparée plutôt que directement sur `main` — décision de méthode de Jules-Antoine (1er septembre), pas une contrainte architecturale. Cohérent avec le fait que cette fonctionnalité n'a aucune dépendance sur la bascule backend (Partie B) : capture et rendu 100% navigateur, aucun stockage serveur même temporaire (voir `extensions-roadmap.md` §1) — le palier gratuit peut donc avancer indépendamment du reste du séquençage ; seul son palier payant dépend des paliers compositeur ci-dessus.

### Périmètre fonctionnel de l'ouverture (au-delà de la simple bascule technique)

Décidé le 27 août, en s'appuyant sur des features déjà conçues dans `extensions-roadmap.md` mais jusque-là non priorisées pour un lancement :
- **Achat de pack depuis l'AdReel** (pas de catalogue global à l'ouverture) — remplace `packs[].buyUrl` (lien externe) par un flux d'achat interne enregistré dans `pack_purchases`.
- **Bibliothèque acheteur** (packs achetés + packs custom réorganisables) — `extensions-roadmap.md` 5.3, conception déjà validée dans le détail, "aucune valeur avant le backend".
- **Vente d'OST façon Bandcamp** (`albums`, achat définitif/téléchargement) — anticipée par Jules-Antoine comme un axe d'attraction studios, potentiellement avant l'horizon initialement prévu. Schéma provisionné (`albums`, `album_purchases`) ; mécanique de lecture auditeur (playlists, "Figer") non provisionnée, conception incomplète à ce stade.
- **Vraie Marketplace organisée** (catalogue, tags, filtres) — repoussée à la Phase 2, une fois "assez de packs en vente" ; `packs.tags` provisionné vide dès maintenant pour éviter une migration ultérieure.
- **Extension `admin-beta-console.html`** (corrigé le 31 août — `admin-analytics.html` référencé initialement ici est obsolète, absorbé par `admin-beta-console.html` depuis la refonte de la bêta multi-GitHub) — comptages directs sur les nouvelles tables Postgres (nombre de comptes total et par profil, d'AdReels, de morceaux, de packs créés/en vente, moyenne par compositeur, tendances de modes de lecture) en complément du suivi comportemental existant (`events.json`/Umami). Implémentation triviale une fois Postgres en place (fait), aucune décision d'architecture supplémentaire requise. **Pas encore construit** — reste sur la liste des tâches.

### Domaine — `layerpitch.com` (statut : acheté le 29 août, bascule DNS vers Cloudflare en cours)

Structure confirmée le 29 août lors de la mise en œuvre de la Partie C :
- **`layerpitch.com` (apex)** → landing page Framer (pré-lancement)
- **`www.layerpitch.com`** → réservé pour le site final (l'app LayerPitch réelle) ; pointera vers GitHub Pages (ou son successeur) au moment du lancement — bascule DNS pure, aucun impact code
- **`beta.layerpitch.com`** → repo GitHub Pages **actuel** (backstage + démo), protégé par Cloudflare Access — voir Partie C pour la séquence complète. Distinct de `www` pour ne pas mélanger le prototype protégé et la future app publique.
- **`media.layerpitch.com`** → bucket R2 (Décision 3), point bloquant levé, configuration du domaine personnalisé sur le bucket lui-même pas encore faite (dépend de la finalisation de la Partie C en cours).

**Prérequis technique** : les nameservers du domaine doivent pointer vers Cloudflare (zone DNS complète), pas seulement un enregistrement isolé — condition pour activer un domaine personnalisé sur R2 et pour Cloudflare Access. Framer n'a pas besoin d'être le gestionnaire des nameservers, un enregistrement DNS chez Cloudflare suffit pour `www`. Nameservers OVH remplacés par ceux de Cloudflare le 29 août — propagation en cours au moment de la rédaction.

**Piège à connaître pour la bascule `www` → GitHub Pages (à anticiper, pas encore rencontré)** : Cloudflare proxifie les enregistrements par défaut (nuage orange), ce qui empêche GitHub de terminer son challenge HTTP pour émettre son certificat Let's Encrypt (HTTPS ne se met jamais en place, risque de boucle de redirection si le TLS Cloudflare est forcé en plus). Mettre l'enregistrement en "DNS only" (nuage gris) le temps que le certificat GitHub soit émis, avant de repasser en proxifié si souhaité. **Ce piège ne s'applique pas à `beta.layerpitch.com`** : Cloudflare Access nécessite justement que l'enregistrement reste proxifié (nuage orange) pour pouvoir s'intercaler — à vérifier concrètement lors de la mise en place du custom domain côté repo GitHub (Partie C) ; si le certificat ne s'émet pas, passer temporairement en DNS only le temps de l'émission, comme pour `www`.

### Roadmap séquencée proposée (12 juillet, non révisée depuis)

Phase 0 (fondation comptes/BDD/stockage) → Phase 1 (Packs V2 minimal, paiement) → Phase 2 (Analytics détaillé, liens multiples, vraie Marketplace) → Phase 3 (Smart Import IA, Interface à prompt) → Phase 4 (Espace Projet, Bibliothèque acheteur vidéo).

### Batterie de tests prévue

*Inspirée d'un échange sur la pratique d'Uncle Bob Martin ("extreme constraints" plutôt que relire chaque ligne de code générée).*

- **Tests unitaires** — priorité sur le moteur audio (`player.js`) : calcul beat/mesure, points de boucle (cas limites y compris), anti-répétition vertical-random et Sfx round robin, ducking. Chaque bug déjà corrigé devient un test de non-régression figé.
- **Tests de sécurité**, une fois le backend en place : isolation stricte entre comptes même via une requête bricolée contre l'API, respect des quotas par palier, respect de la casquette active, non-contournement du calcul de commission.
- **Tests Gherkin** (langage quasi naturel, lisible par Jules-Antoine sans lire de code) — réservés aux comportements qu'il doit pouvoir vérifier lui-même.
- **Tests d'intégration** : parcours backstage → publication → vérification du contenu réellement publié ; parcours de paiement de bout en bout (webhook Stripe simulé).
- **Mutation testing** : une fois la suite ci-dessus posée, teste les tests eux-mêmes.
- **Couverture priorisée, pas uniforme** : élevée sur moteur audio et sécurité/paiement/quotas ; tolérante sur l'apparence.
- **QA manuelle formalisée en checklist**, à répéter avant chaque publication importante.

**Outils concrets retenus** : Vitest (tests unitaires et d'intégration), Gherkin allégé dans Vitest, couverture via Istanbul/c8, Stryker pour le mutation testing (à n'installer qu'une fois une vraie suite existante à challenger).

**Mécanisme d'automatisation** : GitHub Actions lance la suite à chaque push (gratuit à ce volume), qu'il vienne de Jules-Antoine ou d'une session Claude Code.

**Ordre de mise en place recommandé** : (1) Vitest + tests unitaires moteur audio d'abord ; (2) GitHub Actions ; (3) Gherkin, mutation testing, couverture ciblée progressivement.

**Point non négociable avant tout lancement réel** : audit ponctuel par un développeur humain (cloisonnement des comptes, non-fuite de clés, permissions BDD) — pas requis pour la bêta Partie A. Environnement recommandé : Claude Code plutôt qu'un chat classique.

## Partie C — Protection d'accès du repo bêta (Cloudflare Zero Trust Access)

### Problème traité

Le repo GitHub Pages actuel (site + backstage) est aujourd'hui public et sans authentification : n'importe qui tombant sur l'URL peut consulter le prototype et son code source côté client. Objectif : empêcher la découverte non invitée (concurrents, curieux) avant l'ouverture publique, sans bloquer la bascule backend déjà planifiée (Partie B) ni retarder la bêta qualitative (Partie A).

### Solution retenue — Cloudflare Zero Trust Access

Choisie plutôt qu'un repo GitHub privé (GitHub Pro, 4\$/mois) car un repo privé n'empêche pas l'accès au site publié — GitHub Pages reste public par défaut même depuis un repo privé, il protège seulement contre la découverte via l'interface GitHub elle-même (recherche, scraping de code). Cloudflare Access verrouille l'URL publique elle-même.

**Caractéristiques** :
- Gratuit jusqu'à 50 utilisateurs (largement suffisant pour une bêta qualitative d'une dizaine de testeurs).
- Couche d'authentification posée devant le trafic (email + code à usage unique), aucune modification du code du repo.
- Rétention des logs limitée à 24h sur le plan gratuit — sans impact pour cet usage.
- Nécessite que le trafic passe par le réseau Cloudflare (DNS proxifié) — condition qui a motivé l'achat du domaine `layerpitch.com`, un `github.io` par défaut ne le permettant pas.

### Périmètre — `beta.layerpitch.com`

Sous-domaine dédié (voir section Domaine, Partie B) plutôt que l'apex `layerpitch.com`, pour ne pas bloquer la future vitrine Framer publique. Application Zero Trust créée spécifiquement sur `beta.layerpitch.com`, policy "Allow" basée sur une liste d'emails autorisés (Jules-Antoine + contacts de confiance).

### Séquence de mise en œuvre (état au 29 août, en cours)

1. Achat de `layerpitch.com` sur OVH.
2. Ajout du site sur Cloudflare (plan Free).
3. Création du `CNAME beta → julzantoine.github.io` sur Cloudflare, proxy activé.
4. Bascule des nameservers OVH vers Cloudflare (`sloan.ns.cloudflare.com` / `zac.ns.cloudflare.com`) — propagation en cours.
5. *(à faire)* Confirmation de l'activation de la zone Cloudflare.
6. *(à faire)* Renseignement de `beta.layerpitch.com` comme custom domain dans Settings → Pages du repo GitHub — déclenche la redirection automatique de l'URL `github.io` par défaut, condition pour n'avoir qu'un seul point d'accès ("propre", décision actée le 29 août : pas d'URL parallèle non protégée).
7. *(à faire)* Vérification que le site répond correctement en HTTPS sur `beta.layerpitch.com`.
8. *(à faire)* Création de l'Application Zero Trust sur `beta.layerpitch.com`.
9. *(à faire)* Définition de la policy d'accès (liste d'emails).

### Extension envisagée — protection des repos testeurs (`layerpitch-beta`)

**Sujet ouvert le 29 août, non implémenté, à reprendre une fois `beta.layerpitch.com` validé de bout en bout.**

Piste retenue pour éviter de répéter la configuration repo par repo : Cloudflare supporte les enregistrements DNS wildcard et les Applications Zero Trust wildcard, y compris sur le plan gratuit (confirmé le 29 août). Plutôt qu'un CNAME + une Application Access par testeur, mise en place envisagée d'un seul :
- Enregistrement DNS wildcard (ex. `*.testeurs.layerpitch.com`) → GitHub Pages.
- Application Zero Trust wildcard sur le même pattern, avec une policy d'accès.

Pour chaque nouveau testeur, il suffirait alors de renseigner son sous-domaine (ex. `remi.testeurs.layerpitch.com`) dans Settings → Pages de son repo — héritage automatique du DNS et de la protection Access déjà en place, sans nouveau réglage Cloudflare.

**Point non tranché** : liste d'emails commune (chaque testeur voit potentiellement les sous-domaines des autres) vs restriction par testeur (chaque testeur limité à son propre sous-domaine, nécessite une policy plus fine par hostname). À trancher avant implémentation.

### Journal des décisions d'infrastructure

- **12 juillet** — cadrage Partie A/B, six briques manquantes identifiées, script `layerpitch-beta-sync.js` conçu.
- **14 juillet** — décision Cloudflare R2 pour l'audio et la vidéo, Supabase réservé au Postgres ; coûts et marge post-bascule chiffrés ; garde-fous de plafonds actés.
- **13 août** — cache navigateur/CDN sur publication : `data.json` déjà cache-busté (`Date.now()`) ; trou trouvé et corrigé sur `layerpitch-i18n.js`/`layerpitch-help.js` (jamais versionnés, contrairement à `player.js`) — `updateScriptVersions()` généralisée aux trois scripts, `video-test.html` ajouté à la liste des fichiers mis à jour à la publication.
- **27 août** — canal dédié à la bascule backend ouvert. Cinq décisions d'architecture actées : (1) schéma hybride Postgres relationnel + JSONB, avec tables dures sur tout ce qui porte une logique de graphe/transaction (`segment_slots`, `segment_slot_transitions`, achats) ; (2) couche d'abstraction API en service layer JS côté client, RPC Postgres pour la logique métier, Edge Functions réservées aux secrets/appels externes ; (3) migration médias vers R2, bucket unique, domaine personnalisé bloqué en attendant l'achat de `layerpitch.com` ; (4) auth Supabase magic link invite-only, modèle de comptes à profils multiples ; (5) ordre de bascule incrémental (médias → auth → BDD → achats), pattern Strangler Fig. Périmètre fonctionnel d'ouverture élargi pour inclure l'achat de packs depuis l'AdReel, la bibliothèque acheteur, et une entité `Album` provisionnée (vente d'OST façon Bandcamp). Décision explicite de tenir Moodboard Studios (nom et modèle de rémunération) et les mécaniques non finalisées (playlists, "Figer") hors du schéma actuel. Décision de protection d'accès actée le même jour : Cloudflare Zero Trust Access plutôt qu'un repo GitHub privé, sous-domaine `beta.layerpitch.com` dédié.
- **29 août** — correction du schéma de la Décision 1 : `loops` (mode `embranchement-vertical`) déplacé de la piste "table dure façon `segment_slots`" vers JSONB, après vérification sur un premier morceau réel de ce mode. `segment_slots`/`segment_slot_transitions` recentrées explicitement sur le seul mode `sequential`. Aucun impact sur les Décisions 2 à 5.
- **29 août** — achat effectif de `layerpitch.com` sur OVH, point bloquant de la Décision 3 levé (exposition publique réelle possible pour `media.layerpitch.com`). Mise en œuvre de la Partie C démarrée : site ajouté sur Cloudflare, `CNAME beta` créé, bascule des nameservers OVH → Cloudflare lancée (propagation en cours). Décision actée : redirection automatique de l'URL `github.io` par défaut vers `beta.layerpitch.com` une fois le custom domain renseigné côté repo, pour n'avoir qu'un seul point d'accès protégé (pas d'URL parallèle non sécurisée). Piste ouverte (non implémentée) pour étendre la protection aux repos testeurs via DNS et Application Access wildcard.
- **29 août** — Supabase Cloud managé retenu (vs auto-hébergement OVH), tranché dans le canal dédié à la bascule backend. Dernier point d'architecture de la Partie B résolu ; plus aucune décision bloquante avant le démarrage de l'implémentation en Claude Code sur les étapes 1-2 (médias, auth).
- **31 août** — étape 1 (médias vers R2) implémentée, migrée et vérifiée de bout en bout (102 fichiers, `data.json`/`IMAGES_BASE` repointés, test visuel en local concluant). CORS ajouté à la Décision 3 (absent du cadrage initial, trouvé bloquant au test visuel — voir détail dans la Décision 3 et `docs/LAYERPITCH_CHANGELOG.md`). Pipeline d'upload du backstage volontairement laissé sur GitHub pour cette étape (nouveaux uploads médias à geler en attendant une session dédiée à sa réécriture vers R2).
- **31 août** — étape 2 (authentification Supabase) implémentée et testée en isolation (`api/auth.js`, Edge Function `invite-tester`, `auth-test.html`). Flux magic link vérifié de bout en bout (envoi, connexion, session persistée, déconnexion). Inscriptions publiques désactivées. Point ajouté à la Décision 4 : qui a le droit d'appeler `invite-tester` en l'absence de `profiles`/rôle (étape 3 pas encore faite) — vérification interim par email admin en dur, à durcir plus tard. Limite de 2 emails/heure confirmée en pratique (`email rate limit exceeded`), pas seulement anticipée. Intégration au backstage (remplacement effectif du système bêta GitHub) volontairement hors périmètre — prévue "sans coupure" par la Décision 5, donc après validation complète, pas dans cette session.
- **31 août** — étape 3 (base Postgres + migration des données) implémentée et vérifiée. Schéma de 25 tables appliqué (Décision 1), RLS activé partout (lecture publique, écriture `service_role` en attendant la couche CRUD applicative). Migration exécutée puis vérifiée par reconstruction+comparaison profonde de la totalité de `data.json` (27 entités : AdReels, morceaux, Sfx, packs, collections) — au-delà du minimum "un AdReel de test" prévu par la Décision 5. Deux bugs réels trouvés et corrigés par cette vérification avant toute mise en production : conversion silencieuse de chaînes vides en `null` sur plusieurs champs texte, et deux colonnes de `segment_slots` (`bpm`, `customCutFadeSec`) absentes du schéma initial alors qu'utilisées par `player.js`. Le site public ne dépend pas encore de cette base (Décision 5) — couche `api/*.js` et RPC de publication atomique restent à construire.
- **31 août** — couche `api/*.js`/RPC (Décision 2) implémentée et vérifiée (prérequis à l'étape 4, avancé faute de compte Stripe disponible pour l'attaquer directement). `upsert_track`/`upsert_pack`/`upsert_ad_reel` atomiques, `upsert_track` validant le graphe `segmentSlots`/`nextOptions` avant toute écriture — rollback confirmé sur graphe invalide. Deux bugs trouvés uniquement au test via le vrai chemin navigateur/PostgREST (invisibles depuis une connexion Postgres directe) : ambiguïté d'imbrication PostgREST sur la double FK de `segment_slot_transitions`, GRANT `SELECT` manquants pour `anon`/`authenticated` (tables créées hors du flux habituel du dashboard Supabase). `api/publish.js` non construit — pas nécessaire tant que Postgres ne sert pas le site public.
- **31 août** — compte Stripe créé par Jules-Antoine (Managed Payments choisi, 3,5% de frais supplémentaires par transaction contre gestion déléguée des taxes/fraude/support). Étape 4 (logique d'achat) implémentée et vérifiée de bout en bout en mode test réel : `create-checkout-session`, `stripe-webhook`, `api/purchases.js`. Six ajustements/bugs trouvés en testant réellement (pas en théorie) : trois liés spécifiquement à Managed Payments (`payment_method_types` incompatible, code de taxe produit requis, version d'API à fixer explicitement) ; GRANT `service_role` manquants sur les tables (rôle oublié dans le correctif GRANT précédent) ; aucune ligne `profiles` jamais créée à l'inscription (trigger `auth.users` → `public.profiles` absent depuis l'étape 3, ajouté) ; le webhook avalait silencieusement les erreurs d'écriture (paiement confirmé chez Stripe, jamais enregistré côté LayerPitch, sans trace — corrigé pour renvoyer 500 et laisser Stripe réessayer). Prix des packs = valeurs de test, pas une décision de pricing réelle. Les quatre étapes de la bascule backend (Décision 5) sont maintenant posées et vérifiées ; reste l'UI d'achat (non construite) et les vrais prix avant tout lancement.
- **31 août** — UI d'achat construite dans `pack.html` (bouton réel, connexion par lien magique, "déjà acheté") et nouvelle page `library.html` (bibliothèque acheteur), mais **laissées désactivées en production** derrière un interrupteur unique (`PURCHASES_ENABLED = false`) — décision actée avec Jules-Antoine de garder l'affichage "Bientôt disponible" pendant la bêta plutôt que d'exposer le flux Stripe (encore en mode test) aux testeurs actuels ; comportement par défaut revérifié strictement identique à avant. Un bug réel trouvé en testant (variable hors de portée dans la bannière de retour Stripe) corrigé avant de remettre le flag à `false`. `collection.html` non touché (le backend d'achat ne couvre que les packs, pas les collections). `library.html` pas encore lié depuis aucune page publique.
- **31 août — incident production** : Jules-Antoine signale que des AdReels déjà envoyés comme démos ne chargent plus l'audio/Sfx. Cause sans rapport avec cette session (rien n'était encore poussé) : la Partie C a fini de configurer `beta.layerpitch.com` comme domaine personnalisé GitHub Pages, déclenchant une redirection automatique de `julzantoine.github.io` (décision délibérée du 29 août) — `data.json` pointait encore vers l'ancienne URL pour l'audio, rendant le fetch cross-origin et cassant le CORS. Correctif d'urgence poussé seul (17 champs `base` vers `media.layerpitch.com`, déjà migré à l'étape 1) — confirmé réparé en production. Piste de défaire la Partie C écartée (aurait annulé une décision de sécurité volontaire sans réparer le vrai problème). Point trouvé en préparant le correctif, plus urgent que prévu : `layerpitch-backstage.html` réécrivait `track.base`/`sfx.base` vers `github.io` à *chaque* publication — la publication suivante aurait recassé le site. Pipeline d'upload backstage migré vers R2 dans la foulée (signature SigV4 en Web Crypto API, testé réellement) plutôt que de livrer un correctif de données fragile. Détail complet : `docs/LAYERPITCH_CHANGELOG.md`.
- **31 août (soir) — correction d'un oubli de la Décision 1, pas une décision à trancher** : le schéma Postgres créait `composer_profiles` sans jamais rattacher `tracks`/`packs`/`collections`/`ad_reels`/`sfx_library` (ni leurs dossiers) à un propriétaire — LayerPitch est multi-compositeur depuis la conception (business plan, Partie A avec repo isolé par testeur), donc rien à décider, juste à réparer avant le backstage en ligne. `owner_id` ajouté (FK `composer_profiles`, `not null`, contenu existant rattaché au compte de Jules-Antoine — seul compositeur à ce jour, `composer_profile` créé à cette occasion, aucun ne préexistait). Les RPC `upsert_track`/`upsert_pack`/`upsert_ad_reel` vérifient désormais la propriété réelle au lieu de l'admin unique en dur (`is_admin()`, retiré) — testé avec un second compte simulant un compositeur distinct (11/11, y compris le rejet d'une tentative de modification du contenu d'autrui). Lecture publique du site inchangée. Détail complet : `docs/LAYERPITCH_CHANGELOG.md`.
- **1er septembre** — cadrage du séquençage restant de la Partie B avec Claude Code (site public → Postgres, backstage en ligne, comptes multicasquettes, tableaux de bord, fonctionnalités à schéma prêt). `admin-beta-console.html` rapatrié dans le repo (n'y existait pas, fourni depuis une copie locale hors Git). État réel des comptes multicasquettes vérifié (schéma seulement, voir Décision 4 ci-dessus, y compris l'écart à trois profils trouvé en recroisant `extensions-roadmap.md` 5.4). Décision actée : les compositeurs auront aussi des paliers payants `free`/`starter`/`pro`, chiffres à venir côté Jules-Antoine ; implique un futur flux Stripe Billing compositeur, absent à ce jour. Développement du moteur de capture vidéo/rescore (`extensions-roadmap.md` §1, ajouté le même jour suite à l'entretien William Richard) prévu en worktree Git séparée.
- **1er septembre (suite)** — Session B du séquençage backstage en ligne : écriture double actée avec Jules-Antoine (Postgres + GitHub simultanément, jamais l'un sans l'autre, bascule finale décidée par lui seul). Morceaux/packs/AdReels convertis, deux des trois vérifiés en conditions réelles (session réelle, vrai appel RPC), le troisième par pattern seulement (limite d'emails atteinte). Session C démarrée le même jour : RPC `upsert_sfx`/`upsert_collection` construites (manquantes), écriture double étendue aux cinq types de contenu. Bug de course trouvé et documenté (détection de session par lien magique peu fiable quand plusieurs clients Supabase se chargent en même temps, contournable par un rechargement de page) — pas corrigé, candidat pour le nettoyage du client Supabase partagé déjà noté. **Premier vrai test de publication par Jules-Antoine (vrai token GitHub, écriture double)** : un bug réel trouvé (`upsert_track` échouait sur `nextOptions: null`, piège JSONB classique — corrigé, vérifié sur les 14 morceaux réels), puis **publication complète réussie et vérifiée indépendamment des deux côtés** (Postgres et `data.json` sur GitHub identiques, 14/3/6/3/1). L'écriture double est validée en conditions de production réelles. **Lacune trouvée avant de démarrer l'authentification des testeurs, signalée puis corrigée** : aucune lecture Postgres n'était filtrée par compositeur — `list*` renvoyaient le contenu de tous les compositeurs mélangés, invisible tant qu'un seul compte réel existe, mais aurait cassé l'isolation entre testeurs dès le premier second compte. Corrigé : filtrage `owner_id` optionnel sur toutes les fonctions de lecture, site public résolvant l'AdReel/pack/collection consulté en premier pour découvrir son propriétaire, backstage exigeant une session pour lire via Postgres (`ensure_composer_profile()`, nouvelle RPC, provisionne automatiquement à la première connexion). **`settings`/`socials` corrigés dans la foulée, confirmé par Jules-Antoine (personnels, pas globaux)** : migration de schéma (clé primaire singleton → `owner_id`), deux nouvelles RPC `upsert_settings`/`upsert_socials`, écriture double étendue. Deux pièges trouvés en vérifiant, **corrigés le jour même à la demande de Jules-Antoine** (documentés en détail dans `docs/LAYERPITCH_CHANGELOG.md`) : cache de schéma PostgREST non rechargé automatiquement par `scripts/apply-migrations.js` après une migration touchant une structure de table (le script envoie désormais `NOTIFY pgrst, 'reload schema'` après coup) ; `loadPostgresReadScripts()` chargeait les fichiers `api/*.js` sans cache-busting, contrairement à `updateScriptVersions()` pour `player.js`/i18n/help (cache-busting `?v=<timestamp>` ajouté dans les 4 fichiers concernés). **Confirmé avec Jules-Antoine : aucun testeur réel n'existe encore sur le système GitHub actuel** — pas de stratégie de migration individuelle à construire, les nouveaux testeurs partiront directement sur le nouveau système. Interface d'invitation permanente ajoutée dans le backstage (bloc "Inviter un testeur", réutilise `inviteTester()` déjà construit et testé le 31 août, jamais eu d'interface permanente jusqu'ici — appel réel non déclenché, aurait créé un vrai compte et envoyé un vrai email). Reste ouvert, pas tranché : où les testeurs récupèrent concrètement leur copie de `layerpitch-backstage.html` une fois invités (le mécanisme de kit existant via `admin-beta-console.html` reste a priori valable, pas retesté avec le contenu Postgres de cette session — lacune de bundling `api/*.js` trouvée et comblée plus loin dans cette même journée, voir bullet suivant) — reste seulement à valider avec un premier vrai testeur. **Premier vrai essai d'invitation par Jules-Antoine (sa propre seconde adresse, pas un testeur externe)** : lien d'invitation cassé au premier essai (`redirectTo` jamais transmis à `inviteUserByEmail`, retombait sur la Site URL par défaut du projet jamais configurée) — corrigé, `redirectTo` remonté du client jusqu'à l'Edge Function. Message d'erreur générique du SDK corrigé au passage (masquait le vrai message JSON renvoyé par les Edge Functions en échec). **SMTP personnalisé configuré dans la foulée** (Resend, domaine `layerpitch.com` vérifié via Cloudflare, clé API scopée "Sending access" plutôt que "Full access", jamais transmise à Claude Code) — limite de 2 emails/heure portée à 30, confirmée par Supabase. **Isolation multi-compositeur confirmée en conditions réelles** : connecté avec un vrai second compte, la Bibliothèque musicale du backstage affiche bien "Aucun morceau pour l'instant" — zéro fuite du contenu principal. Reste sans urgence : décocher "Enable click tracking" sur le domaine Resend (pas modifiable à la création, à revisiter depuis ses réglages — risque théorique qu'un scanner de sécurité grille un lien à usage unique avant l'utilisateur).
- **1er septembre (suite)** — Lacune du mécanisme de kit testeur comblée : `downloadTesterKit()` (`admin-beta-console.html`) ne bundlait que `layerpitch-backstage.html` + `layerpitch-i18n.js`/`layerpitch-help.js`/`player.js`, jamais le dossier `api/*.js` d'où le backstage charge ses fonctionnalités Postgres (`loadPostgresReadScripts()`, chemins relatifs `./api/*.js`) — un testeur ouvrant le kit en `file://` aurait eu des 404 sur ces scripts au premier essai de connexion par lien magique. Corrigé en listant dynamiquement le contenu du dossier `api/` du repo testeur via l'API GitHub (`contents/api`) plutôt qu'une liste figée, pour rester correct si de nouveaux fichiers `api/*.js` apparaissent sans qu'on pense à retoucher ce script. **Vérifié réellement** (pas seulement en lecture de code) : kit reconstitué à la main avec les 10 fichiers `api/*.js` actuels (dont `purchases.js`, absent de la liste d'origine), servi en local (serveur HTTP statique, la sandbox de cet outil ne permettant pas d'exécuter du JS sur un `file://` hors de son propre dossier de travail) et flux "Envoyer le lien magique" déclenché pour de vrai — les 9 scripts chargés par `loadPostgresReadScripts()` répondent tous 200, aucun 404. La seule erreur observée est un 422 Supabase ("Signups not allowed for this instance"), attendu pour un email de test et sans rapport avec le bundling. Referme la lacune "pas retesté avec le contenu Postgres" signalée plus haut dans cette même entrée — reste ouvert seulement le test de bout en bout avec un vrai testeur invité ouvrant le kit en double-clic.
- **À trancher** : date d'ouverture réelle de la bêta A (dépend de l'essai à blanc + correctif cache) ; policy d'accès Zero Trust pour les repos testeurs (liste commune vs restriction par testeur) ; chiffres des quotas par palier (`plan_quotas`, compositeurs et studios) ; flux Stripe Billing compositeur (nécessaire pour appliquer les paliers) ; UI du profil Fan/Album OST Adaptive Edition (schéma posé le 1er septembre, `extensions-roadmap.md` 5.5, chantier à part entière) ; nom définitif du profil "Fan" (provisoire) ; ADR formalisant la révision de calendrier de la Partie B (documentée ici, ADR séparé à rédiger une fois ces docs poussées sur GitHub).
